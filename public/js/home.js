const porcentagemVoto = document.querySelectorAll('.porcentagemVoto');

porcentagemVoto.forEach((element) =>{
    console.log(element);
    // utilizar style ou getboundReact getComputedStyle para aulterar o with baseado no value
})